# ── Team 2 (CNN) requirements ────────────────────────────────────────────────
# Inherits the platform base — do NOT re-pin mlflow/tensorflow/numpy here, or
# this team's environment can silently drift from the one the shared quality
# gate scores your model in.
-r ../../requirements.txt

# Team-specific dependencies go below this line. Team 2's CNN needs nothing
# beyond the base today; this is the extension point if that changes
# (e.g. a tuner, an augmentation library).
