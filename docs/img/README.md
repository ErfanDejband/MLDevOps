# Screenshots

Drop images here to make them render in the root `README.md` results section.

| Filename | What to capture |
|---|---|
| `experiments.png` | The DagsHub MLflow experiment view, grid-search runs sorted by `val_accuracy` |
| `registry.png` | The `mnist_classifier` registry on prod-tracking, showing version history and the `@champion` alias |
| `quality_gate.png` | A PR where the `evaluate` check ran — ideally one red (regression blocked) and one green |
| `confusion_matrix.png` | The champion model's confusion matrix on the secret test set |

Keep them reasonably sized (≤ 1600px wide) so the README stays quick to load.
