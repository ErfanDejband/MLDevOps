# ── Platform base requirements ───────────────────────────────────────────────
# Everything needed to train a model AND to run the shared quality gate.
# Owned by the "platform" side of the repo (see CICD_MLops/WIKI/ARCHITECTURE.md
# §1) — teams inherit this file rather than re-pinning these versions, so the
# training environment and the environment CI evaluates in can never drift.
#
# Install (from the repo root, into the root .venv):
#   pip install -r requirements.txt

# Experiment tracking + model registry
mlflow>=2.8.0

# Data versioning — DVC with the S3-compatible backend used by DagsHub's remote
dvc[s3]>=3.50.0

# Modelling stack. TensorFlow/Keras live here, not in a team folder, because
# shared/quality_gate.py must load and score whatever model either team
# registered — the gate needs the framework as much as the trainers do.
tensorflow==2.21.0
keras==3.15.0

# Data handling + splits
numpy>=1.24.0
scikit-learn>=1.3.0

# Local credential loading from .env (no-op in CI, which injects real secrets)
python-dotenv>=1.0.0
